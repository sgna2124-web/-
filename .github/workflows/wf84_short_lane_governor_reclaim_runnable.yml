name: 84_short_lane_governor_reclaim_runnable

on:
  workflow_dispatch:

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: python -m pip install --upgrade pip
      - run: pip install pandas numpy
      - run: python scripts/run_backtest_batch_json_only_timeout18_time_reduce_v84_short_lane_governor_reclaim_runnable.py
      - uses: actions/upload-artifact@v4
        with:
          name: 84_short_lane_governor_reclaim_runnable
          path: results_json_only_timeout18_time_reduce_v84_short_lane_governor_reclaim_runnable
